// Code generated by protoc-gen-servora-crud. DO NOT EDIT.

import { ResourceNameError } from "@servora/proto-utils/crud";

function assertResourceSegment(resource: string, variable: string, value: string): void {
  if (value.length === 0 || value.includes("/")) {
    throw new ResourceNameError(`${resource} variable ${variable} must be non-empty and contain no slash`);
  }
}

export type SessionNameParts =
  {
    readonly session: string;
  };

export const SessionName = {
  format(parts: SessionNameParts): string {
    assertResourceSegment("Session", "session", parts.session);
    return ["sessions", parts.session].join("/");
  },
  parse(value: string): SessionNameParts {
    const segments = value.split("/");
    if (segments.length === 2 && segments[0] === "sessions") {
      assertResourceSegment("Session", "session", segments[1]!);
      return {
        session: segments[1]!,
      };
    }
    throw new ResourceNameError(`invalid Session resource name ${value}`);
  },
  tryParse(value: string): SessionNameParts | null {
    try {
      return this.parse(value);
    } catch {
      return null;
    }
  },
} as const;

export const SessionFields = {
  name: "name",
  sessionId: "session_id",
  createTime: "create_time",
  lastSeenTime: "last_seen_time",
  idleExpiresTime: "idle_expires_time",
  absoluteExpiresTime: "absolute_expires_time",
} as const;

export const SessionUpdateFields = {
} as const;
